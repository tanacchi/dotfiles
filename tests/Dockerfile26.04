FROM ubuntu:26.04

RUN apt update -y
RUN apt install -y bash curl git python3 sudo tzdata

ENV TZ=Asia/Tokyo

WORKDIR /root/dotfiles
COPY . /root/dotfiles

RUN python3 scripts/setup/install.py --dry-run --install-mise --install-tools

CMD ["bash"]
